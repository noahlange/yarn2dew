{
  "name": "yarn2dew",
  "version": "0.1.0",
  "type": "module",
  "exports": {
    "default": "./src/index.ts"
  },
  "description": "Compiles Yarn dialogue to Stardew Valley event format.",
  "devDependencies": {
    "@eslint/js": "^9.33.0",
    "@types/bun": "^1.2.19",
    "eslint": "^9.33.0",
    "globals": "^16.3.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.39.1"
  },
  "dependencies": {
    "@mnbroatch/bondage": "^4.2.4",
    "ts-pattern": "^5.8.0"
  },
  "scripts": {
    "build": "bun build ./src/task.ts --outfile yarn2dew --compile",
    "check": "bun run dev:lint && bun run dev:types && bun run dev:pretty && bun test",
    "dev:lint": "bunx eslint .",
    "dev:types": "tsc --noEmit",
    "dev:pretty": "prettier --write 'src/**/*.ts'"
  }
}
